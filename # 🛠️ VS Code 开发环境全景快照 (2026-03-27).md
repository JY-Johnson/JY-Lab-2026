# 🛠️ VS Code 开发环境全景快照 (2026-03-27)

这份文件记录了当前开发环境的完整状态，用于新项目启动时的上下文同步。

## 1. 开发者概况

- **技术背景**：计算机二级、普通话二乙。
- **界面偏好**：VS Code 已完成全中文汉化。

## 2. 目录结构 (Root: C:/workspace/)

采用多根工作区 (Multi-root Workspace) 管理：

- 📂 `py/`：Python 3.14.3 开发环境，已解决 UTF-8 输出乱码。
- 📂 `c/`：C 语言开发环境，已解决 GCC 环境路径及 GBK 终端乱码问题。
- 📂 `web/`：前端开发环境，Node.js (npm) 已配置国内镜像源。

## 3. 核心工具配置

- **编辑器行为**：
  - 开启 `Format On Save` (保存自动格式化)。
  - HTML 默认格式化程序：`Prettier`。
  - 侧边栏已配置隐藏生成的 `**/*.exe` 文件。
- **运行环境 (Code Runner)**：
  - Python 指令已注入环境变量 `PYTHONIOENCODING=utf8`。
  - C 指令已添加编译参数 `-fexec-charset=GBK`。

## 4. 插件阵列 (Plugin Stack)

- **视觉/辅助**：`Material Icon Theme`, `Error Lens` (实时报错), `Live Server` (前端实时预览)。
- **语言支持**：`Python`, `C/C++ Extension Pack`。
- **AI 增强 (核心)**：
  - **GitHub Copilot**：作为代码补全常驻。
  - **Continue (YAML 配置)**：
    - **Provider**: `gemini`
    - **Model**: `gemini-3-flash-preview`
    - **Status**: 已通过 API Key 成功接入 Google AI Studio 引擎，可进行全局代码分析。

---

## 🚀 新项目启动说明 (给 AI 的指令)

“你好！这是我当前经过高度优化的 VS Code 环境。我已经在 Continue 插件中接入了 Gemini 3 Flash，并且 Python 和 C 语言环境均已调试通畅。现在我想开始一个新项目，请基于以上配置为我提供代码指导。”
